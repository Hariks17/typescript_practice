let id: number = 5;
let company: string = "hari";
let isOwner: boolean = true;
let age: any = 24;

let ids: number[] = [1, 2, 3, 4, 5];

let cid: any = 1;

let customerId = cid as number;

function addNumber(x: number, y: number): number {
  return x + y;
}
